apiVersion: 1

datasources:
  - name: Elasticsearch
    type: elasticsearch
    access: proxy
    url: http://elasticsearch:9200
    isDefault: true
    jsonData:
      index: "shiritori-health-pulse-*"
      timeField: "timestamp"
      esVersion: "8.15.0"
      logMessageField: message
      logLevelField: level
